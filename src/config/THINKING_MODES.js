/**
 * The only thinking modes the completion endpoint accepts; 'off' is the default.
 * @type {Readonly<{off: string, extended: string}>}
 */
export const THINKING_MODES = Object.freeze({ off: 'off', extended: 'extended' });
