/**
 * Prefix shared by every localStorage key of this script; settings export and import cover
 * exactly the keys with this prefix.
 * @type {string}
 */
export const STORAGE_KEY_PREFIX = 'claudePlus.';
