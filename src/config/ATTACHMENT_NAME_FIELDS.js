/**
 * Upload fields that may hold a display name, in order of preference.
 * @type {ReadonlyArray<string>}
 */
export const ATTACHMENT_NAME_FIELDS = Object.freeze(['file_name', 'name', 'filename', 'title']);
